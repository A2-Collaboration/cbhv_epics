record (stringout, "$(P):BOX:$(BOXNO):set_$(EELO)_standalone")
{
    field (DTYP, "stream")
    field (OUT, "@$(PROTO) set_eemem_value_standalone($(EEUP)) termBox$(BOXNO)")
    field (PRIO, "HIGH")
}

record (stringin, "$(P):BOX:$(BOXNO):read_$(EELO)")
{
    field (DTYP, "stream")
    field (INP, "@$(PROTO) read_eemem_value($(EEUP)) termBox$(BOXNO)")
    field (SCAN, "I/O Intr")
}

record (stringout, "$(P):BOX:$(BOXNO):eemem")
{
    field (DTYP, "stream")
    field (OUT, "@$(PROTO) eemem_print termBox$(BOXNO)")
    field (PRIO, "HIGH")
}

record (stringout, "$(P):BOX:$(BOXNO):set_$(EELO)")
{
    field (DTYP, "stream")
    field (OUT, "@$(PROTO) set_eemem_value($(EEUP)) termBox$(BOXNO)")
    field (PRIO, "HIGH")
}

record (stringout, "$(P):BOX:$(BOXNO):unprotect_eemem")
{
    field (DTYP, "stream")
    field (OUT, "@$(PROTO) unprotect_eemem termBox$(BOXNO)")
    field (PRIO, "HIGH")
}

record (stringout, "$(P):BOX:$(BOXNO):protect_eemem")
{
    field (DTYP, "stream")
    field (OUT, "@$(PROTO) protect_eemem termBox$(BOXNO)")
    field (PRIO, "HIGH")
}

record (stringout, "$(P):BOX:$(BOXNO):reload_eemem")
{
    field (DTYP, "stream")
    field (OUT, "@$(PROTO) reload_eemem termBox$(BOXNO)")
    field (PRIO, "HIGH")
}